<%= node[:nginx][:log_dir] %>/*.log {
  daily
  missingok
  rotate 52
  dateext
  compress
  delaycompress
  notifempty
  create 640 root adm
  sharedscripts
  postrotate
    [ ! -f /etc/sv/nginx/supervise/pid ] || kill -USR1 `cat /etc/sv/nginx/supervise/pid`
  endscript
}

